--- a/fuzz/asn1.c
+++ b/fuzz/asn1.c
@@ -211,7 +211,7 @@ int FuzzerInitialize(int *argc, char ***argv)
     OPENSSL_init_crypto(OPENSSL_INIT_LOAD_CRYPTO_STRINGS, NULL);
     ERR_get_state();
     CRYPTO_free_ex_index(0, -1);
-    FuzzerSetRand();
+    //FuzzerSetRand();
 
     return 1;
 }
--- a/fuzz/bignum.c
+++ b/fuzz/bignum.c
@@ -17,6 +17,7 @@
 #include <openssl/bn.h>
 #include <openssl/err.h>
 #include "fuzzer.h"
+#include "/root/rosa/fuzzers/aflpp/rosa-trace.h"
 
 
 int FuzzerInitialize(int *argc, char ***argv)
@@ -39,6 +40,8 @@ int FuzzerTestOneInput(const uint8_t *buf, size_t len)
     BIGNUM *b4;
     BIGNUM *b5;
 
+    __ROSA_TRACE_START();
+
     b1 = BN_new();
     b2 = BN_new();
     b3 = BN_new();
--- a/fuzz/client.c
+++ b/fuzz/client.c
@@ -45,7 +45,7 @@ int FuzzerInitialize(int *argc, char ***argv)
     ERR_get_state();
     CRYPTO_free_ex_index(0, -1);
     idx = SSL_get_ex_data_X509_STORE_CTX_idx();
-    FuzzerSetRand();
+    //FuzzerSetRand();
     comp_methods = SSL_COMP_get_compression_methods();
     OPENSSL_sk_sort((OPENSSL_STACK *)comp_methods);
 
--- a/fuzz/server.c
+++ b/fuzz/server.c
@@ -492,7 +492,7 @@ int FuzzerInitialize(int *argc, char ***argv)
     ERR_get_state();
     CRYPTO_free_ex_index(0, -1);
     idx = SSL_get_ex_data_X509_STORE_CTX_idx();
-    FuzzerSetRand();
+    //FuzzerSetRand();
     comp_methods = SSL_COMP_get_compression_methods();
     OPENSSL_sk_sort((OPENSSL_STACK *)comp_methods);
 
--- a/fuzz/x509.c
+++ b/fuzz/x509.c
@@ -19,7 +19,7 @@ int FuzzerInitialize(int *argc, char ***argv)
     OPENSSL_init_crypto(OPENSSL_INIT_LOAD_CRYPTO_STRINGS, NULL);
     ERR_get_state();
     CRYPTO_free_ex_index(0, -1);
-    FuzzerSetRand();
+    //FuzzerSetRand();
     return 1;
 }
 
