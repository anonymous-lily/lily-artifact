diff --git a/lib/util/fatal.c b/lib/util/fatal.c
index a12879ea2..cef2c6aa3 100644
--- a/lib/util/fatal.c
+++ b/lib/util/fatal.c
@@ -200,6 +200,8 @@ warning(const char *errstr, const char *fmt, va_list ap)
 		va_list ap2;
 		int buflen;
 
+// NOTE: the code below causes segfaults, so comment it out.
+#if 0
 		/* Use static buffer if possible, else dynamic. */
 		va_copy(ap2, ap);
 		buflen = vsnprintf(static_buf, sizeof(static_buf), fmt, ap2);
@@ -211,6 +213,7 @@ warning(const char *errstr, const char *fmt, va_list ap)
 		    else
 			buf = static_buf;
 		}
+#endif
 		msgs[nmsgs].msg_type = SUDO_CONV_ERROR_MSG;
 		msgs[nmsgs++].msg = ": ";
 		msgs[nmsgs].msg_type = SUDO_CONV_ERROR_MSG;
diff --git a/plugins/sudoers/check.c b/plugins/sudoers/check.c
index 4a7bb5edd..121ab4416 100644
--- a/plugins/sudoers/check.c
+++ b/plugins/sudoers/check.c
@@ -173,6 +173,13 @@ check_user(int validated, int mode)
     if (sudo_auth_init(closure.auth_pw) == -1)
 	goto done;
 
+/*
+ * NOTE: for fuzzing, we need to run sudo AS sudo. But at the same time, we
+ * need to hit the password check. In order to do that, we need to disable
+ * this entire piece of code that skips the password check if the user is
+ * already sudo, or if they're not elevating privileges.
+ */
+#if 0
     /*
      * Don't prompt for the root passwd or if the user is exempt.
      * If the user is not changing uid/gid, no need for a password.
@@ -200,6 +207,7 @@ check_user(int validated, int mode)
 	    goto done;
 	}
     }
+#endif
 
     ret = check_user_interactive(validated, mode, &closure);
 
diff --git a/src/sudo.c b/src/sudo.c
index 4c6c60e1e..b4a27c522 100644
--- a/src/sudo.c
+++ b/src/sudo.c
@@ -65,6 +65,7 @@
 #include "sudo.h"
 #include "sudo_plugin.h"
 #include "sudo_plugin_int.h"
+#include "/root/rosa/fuzzers/aflpp/rosa-trace.h"
 
 /*
  * Local variables
@@ -157,6 +158,7 @@ main(int argc, char *argv[], char *envp[])
     int submit_optind;
     sigset_t mask;
     debug_decl_vars(main, SUDO_DEBUG_MAIN);
+    __ROSA_TRACE_START();
 
     /* Only allow "sudo" or "sudoedit" as the program name. */
     initprogname2(argc > 0 ? argv[0] : "sudo", allowed_prognames);
