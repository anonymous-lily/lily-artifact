diff --git a/main/php_open_temporary_file.c b/main/php_open_temporary_file.c
index 6fca7e4cab..139e5cf86c 100644
--- a/main/php_open_temporary_file.c
+++ b/main/php_open_temporary_file.c
@@ -49,6 +49,9 @@
 #define P_tmpdir ""
 #endif
 
+#define mkstemp(x) 1
+#define mktemp(x) x
+
 /* {{{ php_do_open_temporary_file */
 
 /* Loosely based on a tempnam() implementation by UCLA */
diff --git a/main/streams/cast.c b/main/streams/cast.c
index 262ec5e38f..9fe8bd5b02 100644
--- a/main/streams/cast.c
+++ b/main/streams/cast.c
@@ -102,8 +102,7 @@ static ssize_t stream_cookie_writer(void *cookie, const char *buffer, size_t siz
 	return php_stream_write(((php_stream *)cookie), (char *)buffer, size);
 }
 
-# ifdef COOKIE_SEEKER_USES_OFF64_T
-static int stream_cookie_seeker(void *cookie, __off64_t *position, int whence)
+static int stream_cookie_seeker(void *cookie, off_t *position, int whence)
 {
 
 	*position = php_stream_seek((php_stream *)cookie, (zend_off_t)*position, whence);
@@ -113,13 +112,6 @@ static int stream_cookie_seeker(void *cookie, __off64_t *position, int whence)
 	}
 	return 0;
 }
-# else
-static int stream_cookie_seeker(void *cookie, zend_off_t position, int whence)
-{
-
-	return php_stream_seek((php_stream *)cookie, position, whence);
-}
-# endif
 
 static int stream_cookie_closer(void *cookie)
 {
diff --git a/sapi/cli/php_cli_server.c b/sapi/cli/php_cli_server.c
index f00d8dee54..6621b32eb3 100644
--- a/sapi/cli/php_cli_server.c
+++ b/sapi/cli/php_cli_server.c
@@ -21,6 +21,7 @@
 #include <stdlib.h>
 #include <fcntl.h>
 #include <assert.h>
+#include "/root/rosa/fuzzers/aflpp/rosa-trace.h"
 
 #ifdef PHP_WIN32
 # include <process.h>
@@ -108,6 +109,22 @@ static pid_t	*php_cli_server_workers;
 static zend_long php_cli_server_workers_max;
 #endif
 
+#define send(sockfd, buf, len, flags) ( \
+    send(-1, buf, len, flags), \
+    errno = 0, \
+    write(1, buf, len) \
+)
+#define closesocket(sockfd) exit(0)
+#define shutdown(sockfd, how) ( \
+    shutdown(-1, how), \
+    errno = 0, \
+    exit(0) \
+)
+
+__AFL_FUZZ_INIT();
+int afl_len = 0;
+unsigned char* afl_buf = NULL;
+
 typedef struct php_cli_server_poller {
 	fd_set rfds, wfds;
 	struct {
@@ -510,7 +527,7 @@ static int sapi_cli_server_startup(sapi_module_struct *sapi_module) /* {{{ */
 
 		if (php_cli_server_workers_max > 1) {
 			zend_long php_cli_server_worker;
-			
+
 			php_cli_server_workers = calloc(
 				php_cli_server_workers_max, sizeof(pid_t));
 			if (!php_cli_server_workers) {
@@ -528,7 +545,7 @@ static int sapi_cli_server_startup(sapi_module_struct *sapi_module) /* {{{ */
 
 				if (pid == FAILURE) {
 					/* no more forks allowed, work with what we have ... */
-					php_cli_server_workers_max = 
+					php_cli_server_workers_max =
 						php_cli_server_worker + 1;
 					return SUCCESS;
 				} else if (pid == SUCCESS) {
@@ -1811,7 +1828,10 @@ static int php_cli_server_client_read_request(php_cli_server_client *client, cha
 	if (client->request_read) {
 		return 1;
 	}
-	nbytes_read = recv(client->sock, buf, sizeof(buf) - 1, 0);
+	if (afl_len > sizeof(buf) - 1) return 0;
+	nbytes_read = afl_len;
+	// Dummy `recv()` to preserve syscall.
+	recv(-1, buf, sizeof(buf) - 1, 0); errno = 0;
 	if (nbytes_read < 0) {
 		int err = php_socket_errno();
 		if (err == SOCK_EAGAIN) {
@@ -1831,10 +1851,10 @@ static int php_cli_server_client_read_request(php_cli_server_client *client, cha
 		return -1;
 	}
 	client->parser.data = client;
-	nbytes_consumed = php_http_parser_execute(&client->parser, &settings, buf, nbytes_read);
+	nbytes_consumed = php_http_parser_execute(&client->parser, &settings, (const char*) afl_buf, nbytes_read);
 	if (nbytes_consumed != (size_t)nbytes_read) {
 		if (php_cli_server_log_level >= PHP_CLI_SERVER_LOG_ERROR) {
-			if (buf[0] & 0x80 /* SSLv2 */ || buf[0] == 0x16 /* SSLv3/TLSv1 */) {
+			if (afl_buf[0] & 0x80 /* SSLv2 */ || afl_buf[0] == 0x16 /* SSLv3/TLSv1 */) {
 				*errstr = estrdup("Unsupported SSL request");
 			} else {
 				*errstr = estrdup("Malformed HTTP request");
@@ -1924,7 +1944,7 @@ static int php_cli_server_client_ctor(php_cli_server_client *client, php_cli_ser
 	{
 		zend_string *addr_str = 0;
 
-		php_network_populate_name_from_sockaddr(addr, addr_len, &addr_str, NULL, 0);
+		addr_str = strpprintf(0, "test");
 		client->addr_str = pestrndup(ZSTR_VAL(addr_str), ZSTR_LEN(addr_str), 1);
 		client->addr_str_len = ZSTR_LEN(addr_str);
 		zend_string_release_ex(addr_str, 0);
@@ -2321,14 +2341,14 @@ static void php_cli_server_dtor(php_cli_server *server) /* {{{ */
 			 int php_cli_server_worker_status;
 
 			 do {
-				if (waitpid(php_cli_server_workers[php_cli_server_worker], 
-						   &php_cli_server_worker_status, 
+				if (waitpid(php_cli_server_workers[php_cli_server_worker],
+						   &php_cli_server_worker_status,
 						   0) == FAILURE) {
 					/* an extremely bad thing happened */
 					break;
 				}
 
-			 } while (!WIFEXITED(php_cli_server_worker_status) && 
+			 } while (!WIFEXITED(php_cli_server_worker_status) &&
 					  !WIFSIGNALED(php_cli_server_worker_status));
 		}
 
@@ -2397,7 +2417,7 @@ static int php_cli_server_ctor(php_cli_server *server, const char *addr, const c
 		goto out;
 	}
 
-	server_sock = php_network_listen_socket(host, &port, SOCK_STREAM, &server->address_family, &server->socklen, &errstr);
+	server_sock = dup2(0, 3);
 	if (server_sock == SOCK_ERR) {
 		php_cli_server_logf(PHP_CLI_SERVER_LOG_ERROR, "Failed to listen on %s:%d (reason: %s)", host, port, errstr ? ZSTR_VAL(errstr) : "?");
 		if (errstr) {
@@ -2542,7 +2562,7 @@ static int php_cli_server_do_event_for_each_fd_callback(void *_params, php_socke
 		php_socket_t client_sock;
 		socklen_t socklen = server->socklen;
 		struct sockaddr *sa = pemalloc(server->socklen, 1);
-		client_sock = accept(server->server_sock, sa, &socklen);
+		client_sock = 0;
 		if (!ZEND_VALID_SOCKET(client_sock)) {
 			if (php_cli_server_log_level >= PHP_CLI_SERVER_LOG_ERROR) {
 				char *errstr = php_socket_strerror(php_socket_errno(), NULL, 0);
@@ -2600,7 +2620,7 @@ static int php_cli_server_do_event_loop(php_cli_server *server) /* {{{ */
 {
 	int retval = SUCCESS;
 	while (server->is_running) {
-		struct timeval tv = { 1, 0 };
+		struct timeval tv = { 0, 0 };
 		int n = php_cli_server_poller_poll(&server->poller, &tv);
 		if (n > 0) {
 			php_cli_server_do_event_for_each_fd(server,
@@ -2731,7 +2751,18 @@ int do_cli_server(int argc, char **argv) /* {{{ */
 
 	zend_signal_init();
 
-	php_cli_server_do_event_loop(&server);
+#ifdef __AFL_HAVE_MANUAL_CONTROL
+	__AFL_INIT();
+#endif  // __AFL_HAVE_MANUAL_CONTROL
+	__ROSA_TRACE_START();
+	afl_buf = __AFL_FUZZ_TESTCASE_BUF;
+
+	while (__AFL_LOOP(UINT_MAX)) {
+		afl_len = __AFL_FUZZ_TESTCASE_LEN;
+
+		php_cli_server_do_event_loop(&server);
+	}
+
 	php_cli_server_dtor(&server);
 	return 0;
 } /* }}} */
